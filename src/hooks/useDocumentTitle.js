import { useLayoutEffect } from 'react';

const useDocumentTitle = (title) => {
  useLayoutEffect(() => {
    if (title) {
      document.title = title;
    } else {
      document.title = 'Mikhuna Peru';
    }
  }, [title]);
};

export default useDocumentTitle;
